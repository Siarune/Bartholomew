function between(min, max) {  
    return Math.floor(
      Math.random() * (max - min) + min
    )
  }

  /*
function randomize(length) {
  return Math.floor(
    Math.random() * (length - 1) + 1
  )
}
*/